/// <summary>
/// Where an attack can be blocked / what it interacts with.
/// Kept as an enum so AI and content tools share one vocabulary.
/// </summary>
public enum MoveHeight
{
    High = 0,
    Mid = 1,
    Low = 2,
}
